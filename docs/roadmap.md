# Localink 开发路线图（进度跟踪）

> 唯一进度源。每个迭代完成后勾选并标注日期。规则见 [AGENTS.md](../AGENTS.md)。

## M0 工程奠基
- [x] M0.1 需求文档 PRD：业务边界、角色、功能清单（`docs/prd.md`）—— 2026-07-24
- [x] M0.2 架构设计文档：模块划分、技术选型表、依赖图（`docs/architecture.md`）—— 2026-07-24
- [x] M0.3 中间件环境安装：MySQL 8 / Redis / Kafka 安装指引 + 连通性验证 —— 2026-07-24
- [ ] M0.4 Maven 多模块骨架：parent pom + 空子模块 + 可启动的空应用（含 Maven Wrapper）
- [ ] M0.5 common 模块：统一返回 Result + 错误码枚举 + 异常体系 + 全局异常处理器
- [ ] M0.6 Git 规范落地：.gitignore、分支策略、Conventional Commits、首个 feature 分支 PR 演示

## M1 基础业务闭环（单库）
- [ ] M1.1 数据库设计文档：ER 图 + 12 张表字段说明（`docs/database.md`）
- [ ] M1.2 单库建表 SQL + HikariCP + MyBatis-Plus 接入
- [ ] M1.3 发送短信验证码（Redis 存验证码 + TTL）
- [ ] M1.4 登录/注册（UUID token + Redis Hash 会话）
- [ ] M1.5 双拦截器鉴权（token 刷新 + 登录校验）+ UserHolder
- [ ] M1.6 商户类型 + 商户 CRUD
- [ ] M1.7 普通优惠券 CRUD
- [ ] M1.8 秒杀券 CRUD（库存 + 开始/结束时间）

## M2 缓存体系（按问题演进）
- [ ] M2.1 Redis 工具框架①：RedisCache 封装（String/Hash/Set/ZSet）
- [ ] M2.2 Redis 工具框架②：Key 治理（KeyManage 枚举 + KeyBuild 前缀规范）
- [ ] M2.3 商户缓存 V1：旁路缓存（先更库再删缓存）
- [ ] M2.4 缓存穿透：空值缓存
- [ ] M2.5 缓存雪崩：TTL 随机抖动
- [ ] M2.6 缓存击穿①：互斥锁重建（自研简单锁，暴露问题）
- [ ] M2.7 缓存击穿②：逻辑过期 + 线程池异步重建
- [ ] M2.8 布隆过滤器框架：Redisson RBloomFilter 封装 + 配置化注册
- [ ] M2.9 布隆过滤器接入：启动初始化灌数据 + 查询链路前置拦截
- [ ] M2.10 Caffeine 本地缓存：双层读链路

## M3 秒杀核心链路
- [ ] M3.1 秒杀 V1：纯 DB 下单（含一人一单 DB 实现）
- [ ] M3.2 JMeter 压测暴露超卖 → 乐观锁修复
- [ ] M3.3 分布式锁框架①：RedissonClient 封装 + 四种锁类型（可重入/公平/读/写）
- [ ] M3.4 分布式锁框架②：@ServiceLock 注解 + SpEL 解析 key + AOP + 超时策略
- [ ] M3.5 一人一单升级：唯一索引兜底 + 分布式锁对比演示
- [ ] M3.6 秒杀 V2①：库存预热 Redis + Lua 原子扣减（hash tag 同槽位）
- [ ] M3.7 MQ 框架：消息模型 + 生产者/消费者模板基类 + Kafka 配置（acks=all、幂等）
- [ ] M3.8 秒杀 V2②：Kafka 异步下单（Lua 扣减 → 发消息 → 消费建单）
- [ ] M3.9 缓存失效广播：Kafka 广播删除各实例本地缓存
- [ ] M3.10 幂等框架：@RepeatExecuteLimit（结果标记 + 本地锁 + 分布式公平锁三级防护）
- [ ] M3.11 消费可靠性：延迟消息丢弃 + 失败回滚 Redis（回滚 Lua：DEL 库存 key 回源）+ 指数退避重试
- [ ] M3.12 对账日志：Redis 流水（Lua hset）+ DB 对账日志表
- [ ] M3.13 限流框架①：令牌桶 Lua + 滑动窗口 Lua
- [ ] M3.14 限流框架②：RateLimitHandler（IP+用户双维度、场景配置、白名单/封禁）
- [ ] M3.15 秒杀前置令牌：申请接口 + 一次性消费（Lua 原子校验删除）

## M4 数据层扩展
- [ ] M4.1 全局 ID 生成器①：雪花算法实现（时钟回拨处理）
- [ ] M4.2 全局 ID 生成器②：Redis Lua 轮转分配 workId/dataCenterId
- [ ] M4.3 分片设计文档：库/表分片键取舍、订单路由表设计
- [ ] M4.4 ShardingSphere 接入：双库建表 + 全业务路由验证
- [ ] M4.5 订单路由表：按 orderId 反查订单位置

## M5 一致性与运营闭环
- [ ] M5.1 对账任务①：Redis 流水 vs DB 订单比对 + PENDING 订单扫描
- [ ] M5.2 对账任务②：差异补偿（回补流水/库存回源）+ 回滚失败表 + 告警
- [ ] M5.3 延迟队列框架：Redisson 延迟队列分片 + 线程池消费
- [ ] M5.4 开抢预通知：开始前 2 分钟延迟消息 + 会员等级人群圈选
- [ ] M5.5 订阅通知①：订阅/取消/状态查询（ZSet 排队 + Hash 状态）
- [ ] M5.6 订阅通知②：取消订单库存回流 → 自动发券给最早订阅者
- [ ] M5.7 店铺每日 Top 买家：ZSet 统计 + 预通知附加圈选

## M6 社区扩展（差异化核心）
- [ ] M6.1 帖子①：发帖/删帖/详情/分页
- [ ] M6.2 帖子②：点赞 + 点赞榜 TopN（ZSet）
- [ ] M6.3 关注：关注/取关/共同关注（Set 交集）
- [ ] M6.4 Feed①：推模式（发帖推送到粉丝收件箱）
- [ ] M6.5 Feed②：关注流滚动分页（按分数滚动）
- [ ] M6.6 Feed③：推挽结合（大V 拉模式 + 普通用户推模式）
- [ ] M6.7 ES 环境安装 + elasticsearch-java client 接入
- [ ] M6.8 ES 同步：发帖 → Kafka → 消费写 ES（复用 MQ 框架）
- [ ] M6.9 搜索接口：全文检索 + 高亮 + 商户聚合筛选
- [ ] M6.10 热榜①：分数模型（点赞/评论/浏览加权 + 时间衰减）
- [ ] M6.11 热榜②：定时快照 + 热榜接口
- [ ] M6.12 内容审核①：DFA 敏感词字典树
- [ ] M6.13 内容审核②：MQ 异步审核状态机（待审→通过/驳回）

## M7 收尾
- [ ] M7.1 全链路压测：JMeter 脚本 + 基线报告
- [ ] M7.2 调优对比：优化前后 QPS/RT 数据报告
- [ ] M7.3 交付文档：部署文档 + 架构图 + README
- [ ] M7.4 面试弹药：30 道自测题 + 简历 bullets 打磨
